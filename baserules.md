```
(∀(x)(∀(y)(
    (\[NUMBER\](x) ∧ \[NUMBER\](y))
    →
    \[NUMBER\]((x + y))
)))

(∀(x)(∀(y)(
    ((x + y) = (y + x))
)))

(∀(x)(∀(y)(∀(z)(
    (((x + y) + z) = (x + (y + z)))
))))

(∀(x)(
    \[NUMBER\](x)
    →
    ((x + 0) = x)
))

() **(WIP)**
```
